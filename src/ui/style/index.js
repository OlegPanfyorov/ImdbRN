import { StyleSheet, Platform } from 'react-native';
import colors from './colors';

const generalStyles = StyleSheet.create({
  exampleStyle: {
    backgroundColor: colors.background,
  },
});

export { colors, generalStyles };
